import Calendar from "../components/Calendar";
// imports the Calendar component and renders it within a div. 
// It serves as a page where the Calendar component can be displayed.
const CalendarPage = () => {
  return (
    <div>
      <Calendar />
    </div>
  );
};

export default CalendarPage;
